<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Omex - Welcome Embed Editor</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;700&display=swap" rel="stylesheet" />
  <style>
    :root {
      --primary-color: #00bfff;
      --secondary-color: #1e90ff;
      --glow-color: rgba(30, 144, 255, 0.8);
      --bg-color-dark: #0a0a0a;
      --text-color-dark: #e0f7ff;
      --input-bg: #111;
      --input-border: #00bfff;
      --shadow-glow: 0 0 20px var(--glow-color);
    }

    * {
      box-sizing: border-box;
      font-family: 'Poppins', sans-serif;
    }

    body {
      margin: 0;
      background: var(--bg-color-dark);
      color: var(--text-color-dark);
      padding: 2rem;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    nav {
      width: 100%;
      background: #111;
      box-shadow: 0 0 20px var(--glow-color);
      border-radius: 12px;
      margin-bottom: 2rem;
      padding: 0 1.5rem;
      display: flex;
      align-items: center;
      justify-content: space-between;
      max-width: 1000px;
      height: 60px;
    }

    nav .logo {
      font-weight: 700;
      font-size: 1.3rem;
      color: var(--primary-color);
      text-shadow: 0 0 10px var(--glow-color);
    }

    nav ul {
      list-style: none;
      display: flex;
      gap: 1.2rem;
      margin: 0;
      padding: 0;
    }

    nav ul li a {
      color: var(--text-color-dark);
      text-decoration: none;
      font-weight: 600;
      padding: 6px 12px;
      border-radius: 6px;
      transition: 0.3s ease;
      background-color: transparent;
    }

    nav ul li a:hover,
    nav ul li a.active {
      color: #00bfff;
      background-color: rgba(0, 191, 255, 0.15);
      box-shadow: 0 0 10px var(--glow-color);
    }

    h1 {
      color: var(--primary-color);
      text-shadow: 0 0 25px var(--glow-color);
      margin-bottom: 1.5rem;
    }

    .container {
      display: flex;
      flex-direction: column;
      gap: 2rem;
      width: 100%;
      max-width: 1000px;
    }

    form {
      background: #111;
      padding: 2rem;
      border-radius: 12px;
      box-shadow: var(--shadow-glow);
      display: flex;
      flex-direction: column;
      gap: 1.2rem;
    }

    label {
      font-weight: 700;
    }

    input[type="text"],
    textarea {
      width: 100%;
      background: var(--input-bg);
      border: 2px solid var(--input-border);
      border-radius: 8px;
      color: var(--text-color-dark);
      padding: 10px 12px;
      font-size: 1rem;
      resize: vertical;
    }

    button {
      background: white;
      color: #000;
      font-weight: 700;
      font-size: 1rem;
      padding: 12px 0;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      transition: 0.3s ease;
    }

    button:hover {
      background: #00bfff;
      color: white;
      box-shadow: 0 0 20px var(--secondary-color);
    }

    .embed-preview {
      background: #111;
      border-radius: 12px;
      box-shadow: var(--shadow-glow);
      padding: 1.5rem;
    }

    .embed-title {
      font-size: 1.5rem;
      color: var(--primary-color);
      margin-bottom: 0.5rem;
    }

    .embed-description {
      font-size: 1.1rem;
      margin-bottom: 1rem;
    }

    .embed-url {
      color: var(--secondary-color);
      font-size: 1rem;
      margin-bottom: 1rem;
      display: inline-block;
    }

    .embed-image {
      max-width: 100%;
      border-radius: 8px;
      margin-top: 10px;
    }

    @media (max-width: 768px) {
      nav {
        flex-direction: column;
        height: auto;
        padding: 1rem;
      }

      nav ul {
        flex-wrap: wrap;
        justify-content: center;
        margin-top: 10px;
      }
    }
  </style>
</head>
<body>
  <nav>
    <div class="logo">Omex</div>
    <ul>
      <li><a href="index.html">Home</a></li>
      <li><a href="invite.html">Invite</a></li>
      <li><a href="commands.html">Commands</a></li>
      <li><a href="premium.html">Premium</a></li>
      <li><a href="support.html">Support</a></li>
      <li><a href="omex-welcome-editor.html" class="active">Welcome Editor</a></li>
    </ul>
  </nav>

  <h1>Omex - Welcome Embed Editor</h1>

  <div class="container">
    <form id="embedForm">
      <label for="title">Title</label>
      <input type="text" id="title" placeholder="Enter embed title">

      <label for="description">Description</label>
      <textarea id="description" rows="4" placeholder="Enter embed description"></textarea>

      <label for="url">URL</label>
      <input type="text" id="url" placeholder="https://example.com">

      <label for="image">Image URL</label>
      <input type="text" id="image" placeholder="https://example.com/image.png">

      <button type="submit">Done</button>
    </form>

    <div class="embed-preview" id="embedPreview">
      <div class="embed-title" id="previewTitle">Embed Title</div>
      <div class="embed-description" id="previewDescription">Embed description will appear here...</div>
      <a href="#" target="_blank" id="previewUrl" class="embed-url" style="display:none;"></a>
      <img src="" alt="Embed Image" class="embed-image" id="previewImage" style="display:none;">
    </div>
  </div>

  <script>
    const titleInput = document.getElementById('title');
    const descInput = document.getElementById('description');
    const urlInput = document.getElementById('url');
    const imageInput = document.getElementById('image');

    const previewTitle = document.getElementById('previewTitle');
    const previewDesc = document.getElementById('previewDescription');
    const previewUrl = document.getElementById('previewUrl');
    const previewImage = document.getElementById('previewImage');

    function updatePreview() {
      const title = titleInput.value.trim();
      const desc = descInput.value.trim();
      const url = urlInput.value.trim();
      const image = imageInput.value.trim();

      previewTitle.textContent = title || 'Embed Title';
      previewDesc.textContent = desc || 'Embed description will appear here...';

      if (url) {
        previewUrl.style.display = 'block';
        previewUrl.href = url;
        previewUrl.textContent = url;
      } else {
        previewUrl.style.display = 'none';
      }

      if (image) {
        previewImage.style.display = 'block';
        previewImage.src = image;
      } else {
        previewImage.style.display = 'none';
      }
    }

    titleInput.addEventListener('input', updatePreview);
    descInput.addEventListener('input', updatePreview);
    urlInput.addEventListener('input', updatePreview);
    imageInput.addEventListener('input', updatePreview);

    document.getElementById('embedForm').addEventListener('submit', async (e) => {
      e.preventDefault();
      alert('Submitted (this is just a placeholder).');
    });

    updatePreview();
  </script>
</body>
</html>
